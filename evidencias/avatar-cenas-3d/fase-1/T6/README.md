# T6 — Smoke REAL Render Local

Status: pronto para execução

- Script: `npm run run:t6:render-local`
- Pré-requisitos: servidor ativo, autenticação válida e infraestrutura de fila/render disponível.
- Evidências geradas pelo runner: `request-response.json`, `metrics.json`, `logs.txt`, `artifact.txt`.

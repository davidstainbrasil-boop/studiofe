
import { WatermarkPosition } from '@/types/watermark.types';

describe('Temp test', () => {
  it('should log the enum', () => {
    console.log(WatermarkPosition);
    expect(true).toBe(true);
  });
});

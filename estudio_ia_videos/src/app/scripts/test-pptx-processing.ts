import { logger } from '@/lib/logger';
// TODO: Script - DISABLED - uses non-existent API
/**
 * 🧪 Script de Teste - PPTX Processing Real
 * NOTE: This script is disabled because it uses deprecated APIs.
 * 
 * Use the API routes instead:
 * - POST /api/pptx/upload - Upload PPTX to Supabase storage
 * - GET /api/pptx/process - Process uploaded PPTX
 */

logger.info('⚠️ Este script está desabilitado.');
logger.info('Use as rotas de API para testar o processamento PPTX:');
logger.info('- POST /api/pptx/upload');
logger.info('- GET /api/pptx/process');

// Export nothing to prevent errors
export {};

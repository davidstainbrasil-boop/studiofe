'use client';

import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const VoicePresetsManager: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gerenciador de Presets de Voz</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Gerenciador de presets e configurações de voz em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};
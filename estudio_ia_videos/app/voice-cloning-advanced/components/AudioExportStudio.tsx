'use client';

import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const AudioExportStudio: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Studio de Exportação de Áudio</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Studio de exportação e processamento de áudio em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
};